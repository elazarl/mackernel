*.img
boot.log
run-in-kernel-boot.log
run-kernel-boot.log
# transient build/scratch dirs created by the runners
.rik-build-*
.mk-build-*
.mk-mod-*
.mk-bundle-*
.mk-fetch-*
# cloud-init seed + host SSH keypair (generated by make-seed.sh)
seed.iso
id_mackernel
id_mackernel.pub
# Python bytecode cache
__pycache__/
# oh-my-claudecode local state
.omc/
# rust server build + runtime
server/target/
server/work/
# react ui build + deps
server/ui/node_modules/
server/ui/dist/

# scaffold-repro.py scratch dirs (PROMPT.md + opencode HOME), created under the repo root
.mk-scaffold-*/
